import 'package:flutter/material.dart';

class PantryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Courses')),
      body: Center(child: Text('Welcome to the Pantry screen!')),
    );
  }
}